apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app: backend
  
spec:
  ports:
    - port: 5000
      targetPort: 5000
	  protocol: TCP
  clusterIP: None
  selector:
    app: backend
